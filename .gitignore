# Editor / tooling
.claude/

# OS / editor junk — never commit these
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store
.vscode/

# Local scratch / originals (optional)
# Keep full-size originals out of the repo; commit only resized web copies.
_originals/
